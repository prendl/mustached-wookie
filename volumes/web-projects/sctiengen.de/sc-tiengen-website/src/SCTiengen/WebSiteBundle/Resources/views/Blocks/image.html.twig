{% extends "SCTiengenWebSiteBundle:Blocks:row.html.twig" %}

{% if block.filter %}
    {% set filter = block.filter %}
{% else %}
    {% set filter = "cmf_block" %}
{% endif %}

{% block row_inner %}
<img src="{{ cmf_media_display_url(block.image) }}" alt="{{ block.label }}" class="fill-block"/>
{% endblock %}
